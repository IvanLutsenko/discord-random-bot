# 🚂 Деплой Discord бота на Railway

## Быстрый старт (5 минут)

### Шаг 1: Создай GitHub репозиторий

1. Зайди на https://github.com/new
2. Назови репозиторий: `discord-random-bot`
3. **НЕ добавляй** README, .gitignore (у нас уже есть)
4. Create repository

### Шаг 2: Загрузи код в GitHub

```bash
# В папке discord_bot выполни:
git init
git add .
git commit -m "Initial commit: Discord random bot"
git branch -M main
git remote add origin https://github.com/твой-username/discord-random-bot.git
git push -u origin main
```

⚠️ **ВАЖНО:** Перед push проверь что `.env` в `.gitignore` (он там есть)!

### Шаг 3: Деплой на Railway

1. **Зарегистрируйся**
   - https://railway.app
   - Sign up with GitHub

2. **New Project**
   - Dashboard → **"New Project"**
   - Выбери **"Deploy from GitHub repo"**
   - Выбери свой репозиторий `discord-random-bot`

3. **Добавь переменную окружения**
   - В проекте Railway → твой сервис → **Variables**
   - Нажми **"+ New Variable"**
   - Variable name: `DISCORD_BOT_TOKEN`
   - Value: `**`
   - **Add**

4. **Deploy!**
   - Railway автоматически задетектит Python
   - Установит зависимости из requirements.txt
   - Запустит бота через Procfile

5. **Проверь логи**
   - Во вкладке **Deployments** → кликни на последний деплой
   - Смотри **View Logs**
   - Должно быть: "🤖 Бот запущен... ✅ Синхронизировано 7 команд"

### Готово! 🎉

Бот теперь онлайн 24/7. Railway даёт:
- ✅ 500 часов в месяц бесплатно (~20 дней)
- ✅ Автоматический рестарт при ошибках
- ✅ Логи и мониторинг
- ✅ GitHub интеграция (push = автодеплой)

---

## Альтернатива: Render.com

Если Railway не подошёл, попробуй Render:

### Деплой на Render

1. **Зарегистрируйся**
   - https://render.com
   - Sign up with GitHub

2. **New → Background Worker**
   - Подключи GitHub репозиторий
   - Name: `discord-random-bot`

3. **Настройки**
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python bot.py`

4. **Environment Variables**
   - Key: `DISCORD_BOT_TOKEN`
   - Value: твой токен
   - Add

5. **Create Background Worker**

Render даёт **750 часов** в месяц бесплатно!

---

## Альтернатива 2: Google Cloud Run (более сложно)

Если нужно больше контроля:

1. Создай Dockerfile
2. Залей на Google Cloud Run
3. Настрой переменные окружения

---

## Troubleshooting

### Бот не запускается на Railway
- Проверь логи (View Logs)
- Проверь что токен добавлен в Variables
- Проверь что Procfile правильный

### "No module named discord"
- Railway должен автоматически установить из requirements.txt
- Проверь что requirements.txt закоммичен в Git

### Бот оффлайн в Discord
- Подожди 2-3 минуты после деплоя
- Проверь логи Railway на ошибки
- Проверь что токен правильный

### "Worker exited with code 1"
- Смотри полные логи — там будет причина
- Обычно это неправильный токен или забыли добавить в Variables

---

## 🔒 Безопасность

После деплоя:
1. ✅ Токен в переменных окружения (не в коде)
2. ✅ `.env` в `.gitignore` (не попадёт в Git)
3. ✅ После тестирования сделай Reset Token в Discord

## 💰 Лимиты бесплатных планов

**Railway:**
- 500 часов/месяц
- 512MB RAM
- Shared CPU

**Render:**
- 750 часов/месяц  
- 512MB RAM
- Автосон после 15 мин неактивности (для web сервисов, не для ботов)

Для Discord бота этого более чем достаточно!

---

## 📊 Мониторинг

В Railway можешь:
- Смотреть логи в реальном времени
- Видеть использование ресурсов
- Настроить алерты
- Смотреть историю деплоев

## 🔄 Автообновление

После настройки:
1. Делаешь изменения в коде локально
2. `git add . && git commit -m "Update" && git push`
3. Railway автоматически задеплоит новую версию

Всё!

# 🎲 Discord Random Bot

Бот для случайного выбора участников Discord сервера. Идеально подходит для розыгрышей призов, выбора докладчиков на митапах, и других случайных выборов.

## ✨ Возможности

### Команды

- `/random` — Выбрать случайного участника сервера
- `/random-online` — Выбрать случайного участника онлайн
- `/random-voice` — Выбрать случайного из голосового канала
- `/random-role` — Выбрать участника с определённой ролью
- `/history` — Показать историю последних выборов
- `/reset` — Сбросить историю (для повторных выборов)
- `/help` — Справка по командам

### Фишки

✅ **Без повторов** — режим `no_repeat` не даст выбрать одного человека дважды  
✅ **Множественный выбор** — параметр `count` позволяет выбрать несколько участников  
✅ **История** — все выборы сохраняются и доступны через `/history`  
✅ **Фильтры** — по статусу (онлайн), по голосовому каналу, по роли  
✅ **Красивые embed** — результаты оформлены в стиле Discord

## 🚀 Быстрый старт

### 1. Создай бота в Discord Developer Portal

1. Зайди на https://discord.com/developers/applications
2. Нажми **New Application**
3. Дай имя боту (например, "Random Bot")
4. Перейди во вкладку **Bot**
5. Нажми **Add Bot**
6. Включи эти **Privileged Gateway Intents**:
   - ✅ Presence Intent
   - ✅ Server Members Intent
   - ✅ Message Content Intent
7. Скопируй **Token** (кнопка Reset Token → Copy)

### 2. Пригласи бота на сервер

1. Перейди во вкладку **OAuth2 → URL Generator**
2. Выбери **Scopes**:
   - ✅ `bot`
   - ✅ `applications.commands`
3. Выбери **Bot Permissions**:
   - ✅ Read Messages/View Channels
   - ✅ Send Messages
   - ✅ Embed Links
   - ✅ Use Slash Commands
4. Скопируй сгенерированный URL и открой в браузере
5. Выбери сервер и добавь бота

### 3. Установи зависимости

```bash
# Создай виртуальное окружение (рекомендуется)
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установи библиотеки
pip install -r requirements.txt
```

### 4. Настрой токен

```bash
# Скопируй пример файла
cp .env.example .env

# Отредактируй .env и вставь свой токен
nano .env  # или любой редактор
```

В файле `.env`:
```
DISCORD_BOT_TOKEN=твой_токен_сюда
```

### 5. Запусти бота

```bash
python bot.py
```

Если всё ок, увидишь:
```
🤖 Бот запущен: Random Bot (ID: 123456789...)
📊 Подключен к 1 серверам
✅ Синхронизировано 7 команд
```

## 📖 Примеры использования

### Розыгрыш на митапе

```
/random-online count:1 no_repeat:True
```
Выберет одного случайного участника онлайн, который ещё не был выбран.

### Выбор 3 докладчиков

```
/random-role role:@Speakers count:3 no_repeat:True
```
Выберет 3 участников с ролью Speakers без повторов.

### Случайный из голосового канала

```
/random-voice count:1
```
Выберет одного участника из твоего текущего голосового канала.

### Посмотреть историю

```
/history limit:20
```
Покажет последние 20 выборов.

### Сбросить историю

```
/reset
```
После этого все участники снова доступны для выбора.

## 🔧 Деплой на хостинг (24/7)

### Вариант А: Railway (Рекомендую 🔥)

1. Зарегистрируйся на https://railway.app
2. Создай новый проект → Deploy from GitHub
3. Выбери репозиторий с ботом
4. Добавь переменную окружения:
   - Key: `DISCORD_BOT_TOKEN`
   - Value: твой токен
5. Railway автоматически задеплоит бота
6. Бесплатный план: 500 часов в месяц (достаточно!)

### Вариант Б: Render

1. Зарегистрируйся на https://render.com
2. New → Background Worker
3. Подключи GitHub репозиторий
4. Build Command: `pip install -r requirements.txt`
5. Start Command: `python bot.py`
6. Добавь Environment Variable:
   - Key: `DISCORD_BOT_TOKEN`
   - Value: твой токен
7. Deploy!

### Вариант В: VPS (полный контроль)

```bash
# Подключись к серверу
ssh user@your-server.com

# Установи Python 3.9+
sudo apt update && sudo apt install python3 python3-pip python3-venv

# Клонируй репозиторий
git clone https://github.com/твой-username/discord-random-bot.git
cd discord-random-bot

# Настрой и запусти
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Создай systemd service для автозапуска
sudo nano /etc/systemd/system/discord-bot.service
```

Содержимое service файла:
```ini
[Unit]
Description=Discord Random Bot
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/home/your-user/discord-random-bot
Environment="DISCORD_BOT_TOKEN=твой_токен"
ExecStart=/home/your-user/discord-random-bot/venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable discord-bot
sudo systemctl start discord-bot
sudo systemctl status discord-bot
```

## 🎯 Кейсы использования

### Для nullptr.talks митапов
```
/random-online count:1 no_repeat:True
```
Выбор победителя розыгрыша мерча из присутствующих онлайн.

### Для GDG Almaty
```
/random-role role:@Attendees count:3 no_repeat:True
```
Выбор 3 случайных участников для раздачи призов от спонсоров.

### Для команды Bereke Business
```
/random-role role:@Android count:1
```
Случайный выбор ревьюера для code review.

### Для голосовых митапов
```
/random-voice count:1
```
Выбор следующего докладчика из тех кто в голосовом.

## 📁 Структура проекта

```
discord-random-bot/
├── bot.py              # Основной файл бота
├── requirements.txt    # Зависимости Python
├── .env.example        # Пример конфигурации
├── history.json        # История выборов (создаётся автоматически)
└── README.md           # Эта документация
```

## 🔒 Безопасность

⚠️ **ВАЖНО:**
- Никогда не коммить `.env` файл в Git!
- Не передавай токен бота никому
- Если токен утёк — сразу сделай Reset Token в Discord Developer Portal

Добавь в `.gitignore`:
```
.env
history.json
venv/
__pycache__/
*.pyc
```

## 🐛 Troubleshooting

### Бот не отвечает на команды
- Проверь что бот онлайн в Discord
- Проверь что включены Privileged Gateway Intents
- Подожди несколько минут после добавления бота (синхронизация команд)

### "Missing Permissions"
- Проверь права бота на сервере
- Бот должен иметь доступ к каналу где вызывается команда

### "Members intent is required"
- Включи **Server Members Intent** в Developer Portal
- Перезапусти бота

## 📝 Лицензия

MIT License — делай с кодом что хочешь!

## 🤝 Контрибьют

Пулл реквесты приветствуются! Для крупных изменений сначала создай issue для обсуждения.

---

Сделано с ❤️ для казахстанского IT-сообщества
